<h1>Instruments#edit</h1>
<h1> <%= @instrument.name.capitalize %></h1>

<%= render 'form', instrument: @instrument %>

<% if policy(@instrument).show? %>
  <%= link_to "Back", instrument_path(@instrument) %>
<% end %>

