<h1>Instruments#show</h1>


<h1><%= @instrument.name.capitalize %></h1>

        <p><%= @instrument.price %> $/day </p>
        <p><%= @instrument.location.capitalize %></p>
        <p><%= @instrument.category.capitalize %></p>
        <p><%= @instrument.description %></p>
        <% if policy(@instrument).edit? %>
          <%= link_to "Edit", edit_instrument_path(@instrument) %>
        <% end %>
        <% if policy(@instrument).new? %>
          <% @booking = Booking.new %>
          <%= link_to "Book this instrument", new_instrument_booking_path(@instrument, @booking) %>
        <% end %>
        <% if policy(Booking).index? %>
          <%= link_to "Bookings of this instrument", bookings_path %>
        <% end %>
        <%= link_to "Back", instruments_path %>


