<%= simple_form_for([@instrument, @booking]) do |f| %>
  <%= f.input :start_date %>
  <%= f.input :end_date %>
  <%= f.submit %>
<% end %>
<% if policy(@instrument).show? %>
  <%= link_to 'Back', instrument_path(@instrument) %>
<% end %>
